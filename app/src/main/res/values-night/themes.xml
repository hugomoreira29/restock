<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark Mode theme -->
    <style name="Base.Theme.ReStock_PG_Dispositivo_Moveis" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/blue_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimaryContainer">@color/dark_primary_container</item>
        <item name="colorOnPrimaryContainer">#A8E6C9</item>

        <!-- Secondary -->
        <item name="colorSecondary">#FFB74D</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSecondaryContainer">#7D4322</item>

        <!-- Tertiary -->
        <item name="colorTertiary">@color/purple_dark</item>
        <item name="colorTertiaryContainer">#4A3570</item>

        <!-- Surfaces -->
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorSurfaceVariant">@color/dark_surface_variant</item>
        <item name="android:colorBackground">@color/dark_bg</item>
        <item name="colorOnBackground">@color/white</item>

        <!-- Atributos personalizados -->
        <item name="productCardCornerRadius">16dp</item>
        <item name="productCardElevation">1dp</item>
    </style>

    <!-- Splash Screen Theme (Night) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_restock_logo</item>
        <item name="windowSplashScreenIconBackgroundColor" tools:targetApi="23">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.ReStock_PG_Dispositivo_Moveis</item>
    </style>
</resources>
